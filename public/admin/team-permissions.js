(()=>{
const $=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const auth=()=>({Authorization:`Bearer ${localStorage.getItem('evkerk-admin-token')||''}`,Accept:'application/json','Content-Type':'application/json'});
async function get(url){const r=await fetch(url,{credentials:'same-origin',cache:'no-store',headers:auth()});const b=await r.json().catch(()=>({}));if(!r.ok||b.ok===false)throw new Error(b.error||`HTTP ${r.status}`);return b}
async function post(url,data){const r=await fetch(url,{method:'POST',credentials:'same-origin',headers:auth(),body:JSON.stringify(data||{})});const b=await r.json().catch(()=>({}));if(!r.ok||b.ok===false)throw new Error(b.error||`HTTP ${r.status}`);return b}
function style(){if($('#teamPermissionStyles'))return;const s=document.createElement('style');s.id='teamPermissionStyles';s.textContent=`.team-perm-card{margin-top:16px}.team-perm-list{display:grid;gap:12px}.team-person{padding:15px;border:1px solid var(--line);border-radius:14px;background:#fff}.team-person-head{display:flex;justify-content:space-between;gap:14px;align-items:flex-start}.team-person h3{margin:0 0 5px}.team-person p{margin:0;color:var(--muted);font-size:12px}.service-checks{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;margin-top:12px}.service-check{display:flex;align-items:center;gap:8px;padding:9px 10px;border:1px solid var(--line);border-radius:10px;background:#fbfdfd;font-size:13px}.service-check input{width:auto}.service-check.planned{border-style:dashed;color:var(--muted)}.service-tag{font-size:11px;color:var(--muted)}.catalog-list{display:flex;gap:7px;flex-wrap:wrap}.catalog-chip{padding:6px 9px;border:1px solid var(--line);border-radius:999px;background:#fff;font-size:12px}.catalog-chip.planned{border-style:dashed;color:var(--muted)}@media(max-width:720px){.service-checks{grid-template-columns:1fr 1fr}}@media(max-width:480px){.service-checks{grid-template-columns:1fr}.team-person-head{display:block}}`;document.head.appendChild(s)}
function prepareExistingForm(){const tab=$('[data-tab="users"]');if(tab)tab.textContent='同工账号';const panel=$('#users');if(!panel)return;const form=$('#adminUserForm');if(form){$('h2',form).textContent='新建同工账号';const note=$('.upload-note',form);if(note)note.textContent='填写同工姓名和邮箱。创建后，同工在官网“同工入口”用邮箱设置自己的密码；旧访问密钥仅保留作迁移/应急。';const email=form.elements.email;if(email){email.required=true;email.closest('label').firstChild.textContent='邮箱';}const role=form.elements.role;if(role){role.value='uploader';role.closest('label').hidden=true;}const submit=form.querySelector('button[type="submit"]');if(submit)submit.textContent='创建同工账号';}
  const listTitle=$('#adminUserList')?.closest('.card')?.querySelector('h2');if(listTitle)listTitle.textContent='同工账号（兼容管理）';
}
function ensureHost(){const panel=$('#users');if(!panel)return null;let host=$('#teamPermissionManager');if(!host){host=document.createElement('section');host.id='teamPermissionManager';host.className='card team-perm-card';host.innerHTML='<h2>同工服事权限</h2><p class="upload-note">这里决定每个同工登录后能看到、能进入哪些服事。技术角色由系统自动兼容，不需要手动理解。</p><div id="teamPermissionList" class="team-perm-list"><p class="hint">正在读取…</p></div>';panel.appendChild(host)}return host}
async function render(){const host=ensureHost();if(!host)return;try{const [usersData,permData]=await Promise.all([get('/api/admin/users'),get('/api/admin/team-services')]);const users=usersData.users||[],catalog=permData.catalog||[],permissions=permData.permissions||{};const list=$('#teamPermissionList');list.innerHTML=users.length?users.map(user=>{const selected=new Set(permissions[user.id]||[]);return`<article class="team-person" data-team-user="${esc(user.id)}"><div class="team-person-head"><div><h3>${esc(user.name)}</h3><p>${esc(user.email||'未填写邮箱')} · ${user.password_ready?'密码已设置':'尚未设置密码'} · ${user.status==='active'?'正常':'已停用'}</p></div><button class="ux-btn primary" type="button" data-save-services>保存服事</button></div><div class="service-checks">${catalog.filter(s=>s.status!=='hidden').map(s=>`<label class="service-check ${s.status==='planned'?'planned':''}"><input type="checkbox" value="${esc(s.id)}" ${selected.has(s.id)?'checked':''}><span>${esc(s.icon||'•')} ${esc(s.title_zh)}${s.status==='planned'?'<small class="service-tag"> · 待接入</small>':''}</span></label>`).join('')}</div><p class="msg" data-service-msg></p></article>`}).join(''):'<p class="hint">还没有同工账号。</p>';list.querySelectorAll('[data-save-services]').forEach(button=>button.onclick=async()=>{const card=button.closest('[data-team-user]'),msg=card.querySelector('[data-service-msg]'),services=[...card.querySelectorAll('input[type="checkbox"]:checked')].map(x=>x.value);button.disabled=true;msg.textContent='保存中…';try{await post(`/api/admin/users/${encodeURIComponent(card.dataset.teamUser)}/services`,{services});msg.textContent='已保存';msg.className='msg ok';setTimeout(render,200)}catch(err){msg.textContent=err.message;msg.className='msg'}finally{button.disabled=false}});
  renderCatalog(host,catalog);
}catch(err){$('#teamPermissionList').innerHTML=`<p class="hint">${esc(err.message)}</p>`}}
}
function renderCatalog(host,catalog){let box=$('#teamServiceCatalog');if(!box){box=document.createElement('div');box.id='teamServiceCatalog';box.innerHTML='<hr><h2>服事目录</h2><p class="hint">目录可以持续扩展。标记“待接入”的服事已经有权限接口，将来接页面或 App 时直接使用同一个 service_id。</p><div class="catalog-list" data-catalog-list></div><form id="newServiceForm" class="grid" style="margin-top:14px"><label>服事 ID<input name="id" placeholder="例如 kitchen_team" required pattern="[a-z0-9_]+"></label><label>中文名称<input name="title_zh" placeholder="例如 餐饮二组" required></label><label>图标<input name="icon" placeholder="🍲"></label><label>状态<select name="status"><option value="planned">待接入</option><option value="active">已接入</option><option value="hidden">隐藏</option></select></label><label class="full">说明<input name="description_zh" placeholder="这个服事负责什么"></label><label class="full">入口地址（尚未开发可留空）<input name="href" placeholder="/team/.../"></label><div class="actions full"><button class="btn" type="submit">新增服事项</button><span class="msg" data-new-service-msg></span></div></form>';host.appendChild(box);$('#newServiceForm').onsubmit=async e=>{e.preventDefault();const form=e.currentTarget,msg=$('[data-new-service-msg]',form),data=Object.fromEntries(new FormData(form));msg.textContent='保存中…';try{await post('/api/admin/team-services',data);form.reset();msg.textContent='已加入服事目录';msg.className='msg ok';render()}catch(err){msg.textContent=err.message;msg.className='msg'}}}
  $('[data-catalog-list]',box).innerHTML=catalog.map(s=>`<span class="catalog-chip ${s.status==='planned'?'planned':''}">${esc(s.icon||'•')} ${esc(s.title_zh)}${s.status==='planned'?' · 待接入':''}</span>`).join('')}
async function boot(){style();prepareExistingForm();try{const me=await get('/api/admin/me');if(me.user?.role!=='owner')return;await render()}catch{}}
boot();new MutationObserver(()=>{prepareExistingForm();if($('#users')?.classList.contains('active')&&!$('#teamPermissionManager'))render()}).observe(document.body,{subtree:true,childList:true,attributes:true,attributeFilter:['class']});
})();
